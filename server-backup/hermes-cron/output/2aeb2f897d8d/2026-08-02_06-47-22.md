# Cron Job: cctv-baseline-6h-monitor

**Job ID:** 2aeb2f897d8d
**Run Time:** 2026-08-02 06:47:22
**Schedule:** every 360m

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

You are a passive monitoring agent for the HAFJET CCTV Worker on office PC 100.121.94.41. Run read-only checks only. Do NOT restart services, edit files/configurations, download models, rotate/delete logs, alter retention, expose the API, or make any HAFJET/Azure/WhatsApp/Telegram changes.

Use SSH as hafizi145. First test connectivity. If unreachable, report that clearly and stop.

Check and report, with every time shown as UTC plus MYT (UTC+8):
1) Worker: exact scoped command `sudo -n /usr/bin/systemctl status cctv-worker` (no extra systemctl args). Report active state, PID, uptime, memory/peak where visible.
2) API health: curl `http://127.0.0.1:8091/health`.
3) Dashboard: HTTP status for `/dashboard`; verify visible HTML labels `Time (MYT)`, `Gender (est.)`, `Age (est.)`; do not claim a label exists without checking.
4) CSV: HTTP status for `/api/events/csv` and verify its header contains `Face Snapshot`.
5) Event data: inspect recent events, count total, events with face_snapshot_path, and post-D.6 events with gender+age. Clearly distinguish historical pre-D.6 missing attributes from any post-D.6 gaps.
6) Face crops: file count and total disk usage of `/mnt/cctv/faces/`.
7) Resources: worker CPU/RSS plus systemd memory/peak. Track trend against prior known baseline: CPU about 45–47%, RSS around 296–435 MB. Escalate if RAM rises continuously across reports without plateau, if RSS exceeds 600 MB, or CPU sustains above 80%.
8) Security: inspect only NEW/latest worker journal lines for RTSP URLs and report masked vs unmasked counts without printing credentials. Expected new format contains `:***@`. Escalate immediately if an unmasked credential appears. Do NOT modify journal history.
9) Errors/anomalies: count recent ERROR/Traceback/Exception lines; separately report RTSP/decode/disconnect warnings as known Tapo TC74 camera-firmware behaviour unless materially worse. Escalate crashes, worker inactive/restarts, API failure, security exposure, or significant resource spike.
10) Retention: use read-only `systemctl list-timers cctv-face-retention.timer --no-pager` (non-sudo) and report next run, expected daily 20:00 UTC / 04:00 MYT.

Privacy: no facial recognition, identity matching, watchlists, embeddings, or biometric tracking. Gender/age remain display-only AI estimates (known ~75% accuracy; not facts). Keep report concise and factual. If all stable, state BASELINE STABLE; otherwise clearly state ESCALATION REQUIRED and why.

## Response

All checks complete. Here is the monitoring report for **2026-08-02 ~06:45 UTC (14:45 MYT)**.

---

# HAFJET CCTV Worker — Monitoring Report
**Host:** hafjet-pc-office (100.121.94.41) · **Check time:** 06:45 UTC / 14:45 MYT

## ⚠️ VERDICT: **ESCALATION REQUIRED — Memory spike**

---

## 1) Worker Service
- **Command:** `sudo -n /usr/bin/systemctl status cctv-worker` ✅ (exact scoped, NOPASSWD ok)
- **State:** `active (running)` · **PID:** 2612176 (python, `-m app.main`)
- **Uptime:** since 2026-08-01 09:17:45 UTC (17:17:45 MYT) — ~21h, **no restarts in 24h** (0 Start/Stop markers)
- **Memory:** 606.8M systemd (peak 609.6M) · **ps RSS:** 596.2 MB

## 2) API Health
- `GET /health` → **HTTP 200**, `{"status":"ok","camera_configured":true,"camera_enabled":true,"camera_name":"entrance","free_disk_gb":344.5,"free_disk_pct":93.6,"models_present":true}` ✅

## 3) Dashboard
- `GET /dashboard` → **HTTP 200** ✅
- `Time (MYT)` label: **PRESENT** (20 occurrences)
- `Gender (est.)` exact label: **NOT PRESENT** — HTML uses label `Gender` with title `AI estimate… display only` (no `(est.)` in label text)
- `Age (est.)` exact label: **NOT PRESENT as label** — values render as badges `15-20 (est.)`, `25-32 (est.)`, etc. with AI-estimate title. Age/gender columns exist and are populated; only the literal label strings differ.

## 4) CSV Export
- `GET /api/events/csv` → **HTTP 200** ✅ · Header contains **`Face Snapshot`** (last column): `Event ID,Camera,Type,Timestamp (UTC),Confidence,Snapshot,Face Snapshot`

## 5) Event Data (n=958)
| Metric | Count |
|---|---|
| Total events | 958 |
| With `face_snapshot_path` | 419 |
| With gender+age_range | 344 |
- **D.6 boundary:** first gender+age event at **2026-07-27 04:16:49 UTC (12:16 MYT)**
- **Post-D.6:** 706 events → 344 snapshots, 344 with gender+age. **True post-D.6 gaps: 0** — all 362 missing-attribute events also lack face snapshots (expected: attributes only exist when a face is detected). Last 24h: 41 events, 20 with snap, **0 snap-without-attributes**. No regression.

## 6) Face Crops
- `/mnt/cctv/faces/` → **419 files, 9.4 MB** ✅ (matches 419 face_snapshot_path events)

## 7) Resources — ⚠️ Memory elevated
- **CPU:** 45.6% snapshot; ~46.7% avg (9h48m CPU / 21h uptime) → **within baseline 45–47%** ✅
- **RSS:** **596.2 MB** (systemd 606.8M, peak 609.6M) vs **prior baseline 296–435 MB** → **+37% above top of baseline band**
- Memory sits ~2.8 MB below its peak (609.6M) — i.e., at high-water mark, no sign of release.
- Hard trigger (RSS > 600 MB) **not yet breached** (596 MB), but this is a **significant sustained spike** well outside the documented baseline band → **ESCALATION: recommend investigating possible memory growth/leak in the worker process** (e.g., OpenCV/ffmpeg frame buffers, reconnect loop accumulation).

## 8) Security — RTSP URL scan (last 1h, journal read-only)
- RTSP lines: **56** · Masked (`:***@`): **56** · **Unmasked: 0** ✅ — no credential exposure. No journal modifications performed.

## 9) Errors / Anomalies
- **ERROR/Traceback/Exception: 0** in last 1h and last 24h ✅
- Stream warnings (1h): **28 cycles** of `End of video stream or read error` → `Stream disconnected. Reconnecting in 5 seconds...` (~every 2 min) — **consistent with known Tapo TC74 camera-firmware behaviour**, not materially worse. Noted for the record.
- No crashes, no restarts, API healthy, no security exposure.

## 10) Retention Timer
- `cctv-face-retention.timer`: **next run Sun 2026-08-02 20:00:00 UTC = 2026-08-03 04:00:00 MYT** (in ~13h) ✅ on schedule (last ran 2026-08-01 20:00:40 UTC).

---

## Summary
| Check | Status |
|---|---|
| Worker active / API / Dashboard / CSV | ✅ Stable |
| Labels `Time (MYT)` | ✅ Present |
| Labels `Gender (est.)` / `Age (est.)` | ⚠️ Literal strings absent; `Gender`/`Age` columns present with AI-estimate titles |
| Post-D.6 attribute gaps | ✅ 0 true gaps |
| Face crops / disk / retention | ✅ Stable |
| RTSP credential exposure | ✅ None |
| **Memory (RSS 596 MB vs baseline 296–435 MB)** | ⚠️ **ESCALATION REQUIRED** |

**Recommendation:** Worker, API, data integrity, and security are all stable. The single escalation item is **worker memory**: RSS ~596 MB (systemd 606.8M, peak 609.6M), ~37% above the documented 296–435 MB baseline and sitting at its peak — trending toward the 600 MB hard threshold. No action taken (read-only mandate); advise tracking next report for plateau vs continued growth and investigating the reconnect-loop memory footprint.
