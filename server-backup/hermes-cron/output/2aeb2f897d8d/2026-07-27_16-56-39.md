# Cron Job: cctv-baseline-6h-monitor

**Job ID:** 2aeb2f897d8d
**Run Time:** 2026-07-27 16:56:39
**Schedule:** every 360m

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

You are a passive monitoring agent for the HAFJET CCTV Worker on office PC 100.121.94.41. Run read-only checks only. Do NOT restart services, edit files/configurations, download models, rotate/delete logs, alter retention, expose the API, or make any HAFJET/Azure/WhatsApp/Telegram changes.

Use SSH as hafizi145. First test connectivity. If unreachable, report that clearly and stop.

Check and report, with every time shown as UTC plus MYT (UTC+8):
1) Worker: exact scoped command `sudo -n /usr/bin/systemctl status cctv-worker` (no extra systemctl args). Report active state, PID, uptime, memory/peak where visible.
2) API health: curl `http://127.0.0.1:8091/health`.
3) Dashboard: HTTP status for `/dashboard`; verify visible HTML labels `Time (MYT)`, `Gender (est.)`, `Age (est.)`; do not claim a label exists without checking.
4) CSV: HTTP status for `/api/events/csv` and verify its header contains `Face Snapshot`.
5) Event data: inspect recent events, count total, events with face_snapshot_path, and post-D.6 events with gender+age. Clearly distinguish historical pre-D.6 missing attributes from any post-D.6 gaps.
6) Face crops: file count and total disk usage of `/mnt/cctv/faces/`.
7) Resources: worker CPU/RSS plus systemd memory/peak. Track trend against prior known baseline: CPU about 45–47%, RSS around 296–435 MB. Escalate if RAM rises continuously across reports without plateau, if RSS exceeds 600 MB, or CPU sustains above 80%.
8) Security: inspect only NEW/latest worker journal lines for RTSP URLs and report masked vs unmasked counts without printing credentials. Expected new format contains `:***@`. Escalate immediately if an unmasked credential appears. Do NOT modify journal history.
9) Errors/anomalies: count recent ERROR/Traceback/Exception lines; separately report RTSP/decode/disconnect warnings as known Tapo TC74 camera-firmware behaviour unless materially worse. Escalate crashes, worker inactive/restarts, API failure, security exposure, or significant resource spike.
10) Retention: use read-only `systemctl list-timers cctv-face-retention.timer --no-pager` (non-sudo) and report next run, expected daily 20:00 UTC / 04:00 MYT.

Privacy: no facial recognition, identity matching, watchlists, embeddings, or biometric tracking. Gender/age remain display-only AI estimates (known ~75% accuracy; not facts). Keep report concise and factual. If all stable, state BASELINE STABLE; otherwise clearly state ESCALATION REQUIRED and why.

## Response

All data collected. Here is the complete report.

---

## HAFJET CCTV Worker — Monitoring Report
**Office PC:** `100.121.94.41` | **Time:** ~2026-07-27 16:56 UTC / **28 Jul 00:56 MYT**

---

### 1. Worker Status
| Field | Value |
|---|---|
| State | **active (running)** |
| PID | 9715 |
| Uptime | ~2h 34min (since 14:22 UTC / 22:22 MYT) |
| Memory | 274.6 MB (peak: 278 MB) |
| Tasks | 25 |

✅ Running, no restarts detected.

---

### 2. API Health (`/health`)
```json
{"status":"ok","camera_configured":true,"camera_enabled":true,"camera_name":"entrance",
 "free_disk_gb":348.7,"free_disk_pct":94.7,"model_dir_exists":true,"models_present":true}
```
✅ All green. 348.7 GB free (94.7%).

---

### 3. Dashboard (`/dashboard`)
- HTTP: **200** ✅
- Labels verified in HTML:
  - `Gender` — **present** (displayed as `<span class='media-label'>Gender</span>`) ⚠️ *missing `(est.)` suffix*
  - `Age` — **present** (displayed as `<span class='media-label'>Age</span>`) ⚠️ *missing `(est.)` suffix*
  - `Time (MYT)` — **NOT present** as an explicit text label. The time column uses CSS class `.event-time .myt` and renders `MYT` as a `<span class='timezone'>` badge (e.g., `21:14:53 MYT`), but the literal string `Time (MYT)` does not appear.

---

### 4. CSV (`/api/events/csv`)
- HTTP: **200** ✅
- Header: `Event ID,Camera,Type,Timestamp (UTC),Confidence,Snapshot,Face Snapshot`
- **`Face Snapshot` column confirmed** ✅

---

### 5. Event Data (50 total)
| Metric | Count |
|---|---|
| Total events | 50 |
| With `face_snapshot_path` | 29 |
| With `gender` | 29 |
| With `age_range` | 29 |
| With both gender + age | 29 |

**Breakdown:**
- **29/50 events** have gender + age populated → these are **post-D.6** events with full attributes.
- **21/50 events** have `null` for gender, age_range, and face_snapshot_path → these are **pre-D.6 historical events** (before gender/age inference was deployed). This is expected — no post-D.6 gaps detected.
- Latest post-D.6 events (UTC): `06:57` (Male, 0–2), `06:53` (Female, 60+), `04:44` (Male, 15–20) — all with face crops.

---

### 6. Face Crops (`/mnt/cctv/faces/`)
- **107 files** | **3.3 MB** total

---

### 7. Resources
| Metric | Current | Baseline | Status |
|---|---|---|---|
| Worker CPU | **45.9%** | 45–47% | ✅ within range |
| Worker RSS | **318.7 MB** | 296–435 MB | ✅ within range |
| System RAM used | 1,099 MB / 17,357 MB | — | ✅ healthy |

No upward trend. RSS stable (~319 MB vs systemd-reported 274.6 MB — the difference is Python interpreter overhead vs. cgroup accounting). No escalation.

---

### 8. Security — RTSP Credential Audit
- RTSP lines in last 2h: **all masked** (`:***@` format)
- **Unmasked credentials: 0** ✅
- No credential exposure. No escalation.

---

### 9. Errors & Anomalies
| Category | Count (2h) | Assessment |
|---|---|---|
| ERROR / Traceback / Exception | **0** | ✅ Clean |
| Stream disconnect/reconnect | **46** | ⚠️ Known Tapo TC74 behaviour |
| h264 decode errors | **1** | ⚠️ Known Tapo TC74 firmware issue |

Disconnects occur every ~2–7 minutes — consistent with historical TC74 pattern. Not materially worse. No crash, no restart, no API failure.

---

### 10. Retention Timer
```
NEXT: Mon 2026-07-27 20:00:00 UTC / Tue 28 Jul 04:00 MYT
LAST: Sun 2026-07-26 20:00:00 UTC (PASSED)
```
✅ On schedule — next run in ~3 hours.

---

## VERDICT: **BASELINE STABLE**

All core services operational. No crashes, no credential leaks, resource usage within norms. The 46 stream disconnects are the known Tapo TC74 pattern. Minor note: Dashboard labels show `Gender` / `Age` without the `(est.)` suffix and `Time (MYT)` is rendered via CSS/timezone badge rather than as an explicit text label — cosmetic, non-blocking. No escalation required.
