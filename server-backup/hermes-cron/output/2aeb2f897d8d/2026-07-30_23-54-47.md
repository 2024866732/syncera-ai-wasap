# Cron Job: cctv-baseline-6h-monitor

**Job ID:** 2aeb2f897d8d
**Run Time:** 2026-07-30 23:54:47
**Schedule:** every 360m

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

You are a passive monitoring agent for the HAFJET CCTV Worker on office PC 100.121.94.41. Run read-only checks only. Do NOT restart services, edit files/configurations, download models, rotate/delete logs, alter retention, expose the API, or make any HAFJET/Azure/WhatsApp/Telegram changes.

Use SSH as hafizi145. First test connectivity. If unreachable, report that clearly and stop.

Check and report, with every time shown as UTC plus MYT (UTC+8):
1) Worker: exact scoped command `sudo -n /usr/bin/systemctl status cctv-worker` (no extra systemctl args). Report active state, PID, uptime, memory/peak where visible.
2) API health: curl `http://127.0.0.1:8091/health`.
3) Dashboard: HTTP status for `/dashboard`; verify visible HTML labels `Time (MYT)`, `Gender (est.)`, `Age (est.)`; do not claim a label exists without checking.
4) CSV: HTTP status for `/api/events/csv` and verify its header contains `Face Snapshot`.
5) Event data: inspect recent events, count total, events with face_snapshot_path, and post-D.6 events with gender+age. Clearly distinguish historical pre-D.6 missing attributes from any post-D.6 gaps.
6) Face crops: file count and total disk usage of `/mnt/cctv/faces/`.
7) Resources: worker CPU/RSS plus systemd memory/peak. Track trend against prior known baseline: CPU about 45–47%, RSS around 296–435 MB. Escalate if RAM rises continuously across reports without plateau, if RSS exceeds 600 MB, or CPU sustains above 80%.
8) Security: inspect only NEW/latest worker journal lines for RTSP URLs and report masked vs unmasked counts without printing credentials. Expected new format contains `:***@`. Escalate immediately if an unmasked credential appears. Do NOT modify journal history.
9) Errors/anomalies: count recent ERROR/Traceback/Exception lines; separately report RTSP/decode/disconnect warnings as known Tapo TC74 camera-firmware behaviour unless materially worse. Escalate crashes, worker inactive/restarts, API failure, security exposure, or significant resource spike.
10) Retention: use read-only `systemctl list-timers cctv-face-retention.timer --no-pager` (non-sudo) and report next run, expected daily 20:00 UTC / 04:00 MYT.

Privacy: no facial recognition, identity matching, watchlists, embeddings, or biometric tracking. Gender/age remain display-only AI estimates (known ~75% accuracy; not facts). Keep report concise and factual. If all stable, state BASELINE STABLE; otherwise clearly state ESCALATION REQUIRED and why.

## Response

**HAFJET CCTV Worker Passive Monitoring Report**  
*Generated: 2026-07-31 07:50 MYT (2026-07-30 23:50 UTC)*  
*Target: office PC 100.121.94.41 (hafizi145 via SSH)*

---

### 1) Worker Service (`cctv-worker`)
| Field | Value |
|-------|-------|
| **Active State** | `active (running)` |
| **PID** | 58914 |
| **Uptime** | 18h (since 2026-07-30 04:50:45 UTC / 12:50:45 MYT) |
| **Memory (systemd)** | 934.5 MB **(peak: 939 MB)** ⚠️ **EXCEEDS 600 MB THRESHOLD** |
| **CPU (systemd)** | 8h 45m 1s total |
| **Current CPU%** | 46% (baseline: 45–47%) ✅ |
| **Current RSS (ps)** | ~584 MB |

---

### 2) API Health (`/health`)
```
HTTP 200 OK
{
  "status": "ok",
  "camera_configured": true,
  "camera_enabled": true,
  "camera_name": "entrance",
  "free_disk_gb": 347.8,
  "free_disk_pct": 94.5,
  "models_present": true
}
```
✅ Healthy

---

### 3) Dashboard (`/dashboard`)
- **HTTP Status**: 200 OK
- **Verified Labels Present**:
  - `Time (MYT)` ✅
  - `Gender` (displayed as "Gender (est.)" with badge tooltips) ✅
  - `Age` (displayed as "Age (est.)" with badge tooltips) ✅

---

### 4) CSV Export (`/api/events/csv`)
- **HTTP Status**: 200 OK
- **Header**: `Event ID,Camera,Type,Timestamp (UTC),Confidence,Snapshot,Face Snapshot` ✅ (contains `Face Snapshot`)

---

### 5) Event Data (last 500 events via `/api/events?limit=500`)
| Metric | Count |
|--------|-------|
| **Total events** | 500 |
| **With `face_snapshot_path`** | 237 |
| **With `gender` + `age_range` (post-D.6)** | 237 |
| **Pre-D.6 gaps (face but no gender/age)** | 0 ✅ |

All face-detected events in the recent window carry gender+age estimates. Historical pre-D.6 events (older than the 500-event window) remain without attributes — expected.

---

### 6) Face Crops (`/mnt/cctv/faces/`)
- **Files**: 347
- **Disk Usage**: 7.7 MB

---

### 7) Resource Trend vs Baseline
| Metric | Baseline | Current | Status |
|--------|----------|---------|--------|
| **CPU %** | 45–47% | 46% | ✅ Stable |
| **RSS (ps)** | 296–435 MB | ~584 MB | ⚠️ Rising |
| **systemd Memory** | — | 934.5 MB (peak 939 MB) | 🔴 **EXCEEDS 600 MB** |
| **systemd Peak** | — | 939 MB | 🔴 **CONTINUOUS GROWTH** |

**ESCALATION REQUIRED**: Systemd reports 934.5 MB current / 939 MB peak — well above the 600 MB escalation threshold and trending upward from the prior 296–435 MB baseline. Worker has not restarted (18h uptime); memory has not plateaued.

---

### 8) Security — RTSP Credential Masking (journalctl recent)
- **Lines inspected**: 20 recent reconnection log entries
- **Masked format (`:***@`)**: 20 / 20 ✅
- **Unmasked credentials**: 0 ✅

No credential exposure detected.

---

### 9) Errors / Anomalies (last 200 journal lines)
| Category | Count | Notes |
|----------|-------|-------|
| **ERROR / Traceback / Exception** | 0 | None |
| **RTSP / decode / disconnect WARN** | ~30 | Expected Tapo TC74 firmware behaviour (stream teardowns, h264 MB decode errors). Frequency stable, not materially worse. |
| **Worker crashes / restarts** | 0 | Single continuous 18h run |
| **API failures** | 0 | All endpoints healthy |

---

### 10) Retention Timer (`cctv-face-retention.timer`)
```
NEXT                        LEFT          LAST                      PASSED  UNIT                         ACTIVATES
Fri 2026-07-31 20:00:00 UTC  20h left     Thu 2026-07-30 20:00:40 UTC  3h 51m ago  cctv-face-retention.timer  cctv-face-retention.service
```
- **Next run**: 2026-07-31 20:00 UTC / **04:00 MYT** ✅ (daily as expected)

---

## SUMMARY
**🔴 ESCALATION REQUIRED** — Worker memory (systemd) at **934.5 MB / peak 939 MB**, exceeding the 600 MB threshold and showing continuous growth from the 296–435 MB baseline. No restart or plateau observed.  
All other checks: **BASELINE STABLE** (API, dashboard, CSV, events, face crops, security masking, retention timer, error rate).
