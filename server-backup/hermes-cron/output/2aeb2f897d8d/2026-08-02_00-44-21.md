# Cron Job: cctv-baseline-6h-monitor

**Job ID:** 2aeb2f897d8d
**Run Time:** 2026-08-02 00:44:20
**Schedule:** every 360m

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

You are a passive monitoring agent for the HAFJET CCTV Worker on office PC 100.121.94.41. Run read-only checks only. Do NOT restart services, edit files/configurations, download models, rotate/delete logs, alter retention, expose the API, or make any HAFJET/Azure/WhatsApp/Telegram changes.

Use SSH as hafizi145. First test connectivity. If unreachable, report that clearly and stop.

Check and report, with every time shown as UTC plus MYT (UTC+8):
1) Worker: exact scoped command `sudo -n /usr/bin/systemctl status cctv-worker` (no extra systemctl args). Report active state, PID, uptime, memory/peak where visible.
2) API health: curl `http://127.0.0.1:8091/health`.
3) Dashboard: HTTP status for `/dashboard`; verify visible HTML labels `Time (MYT)`, `Gender (est.)`, `Age (est.)`; do not claim a label exists without checking.
4) CSV: HTTP status for `/api/events/csv` and verify its header contains `Face Snapshot`.
5) Event data: inspect recent events, count total, events with face_snapshot_path, and post-D.6 events with gender+age. Clearly distinguish historical pre-D.6 missing attributes from any post-D.6 gaps.
6) Face crops: file count and total disk usage of `/mnt/cctv/faces/`.
7) Resources: worker CPU/RSS plus systemd memory/peak. Track trend against prior known baseline: CPU about 45–47%, RSS around 296–435 MB. Escalate if RAM rises continuously across reports without plateau, if RSS exceeds 600 MB, or CPU sustains above 80%.
8) Security: inspect only NEW/latest worker journal lines for RTSP URLs and report masked vs unmasked counts without printing credentials. Expected new format contains `:***@`. Escalate immediately if an unmasked credential appears. Do NOT modify journal history.
9) Errors/anomalies: count recent ERROR/Traceback/Exception lines; separately report RTSP/decode/disconnect warnings as known Tapo TC74 camera-firmware behaviour unless materially worse. Escalate crashes, worker inactive/restarts, API failure, security exposure, or significant resource spike.
10) Retention: use read-only `systemctl list-timers cctv-face-retention.timer --no-pager` (non-sudo) and report next run, expected daily 20:00 UTC / 04:00 MYT.

Privacy: no facial recognition, identity matching, watchlists, embeddings, or biometric tracking. Gender/age remain display-only AI estimates (known ~75% accuracy; not facts). Keep report concise and factual. If all stable, state BASELINE STABLE; otherwise clearly state ESCALATION REQUIRED and why.

## Response

All checks complete. Compiling the final report.

---

# HAFJET CCTV Worker — Laporan Pemantauan (Read-Only)
**Check time:** UTC 2026-08-02 00:40:27 | **MYT** 2026-08-02 08:40:27
**Host:** 100.121.94.41 (hafjet-pc-office) · SSH: connected ✓

## ⚠️ ESCALATION REQUIRED — Worker memory jauh melebihi baseline

**Sebab:** RSS worker naik ke **~589 MB** (ps VmRSS 602,924 kB) berbanding baseline terdahulu **296–435 MB**. Peak systemd **590.2 MB**. Hampir mencecah ambang 600 MB — cuma tinggal ~11 MB. Ini significant resource spike; perlu disahkan sama ada plateau atau terus naik pada laporan seterusnya. CPU kekal normal (45.7%), jadi ini isu memori sahaja, bukan CPU.

---

## 1) Worker Service — ✅ Aktif
- **State:** `active (running)` · Main PID **2612176** (python -m app.main) · **NRestarts: 0**
- **Uptime:** since 2026-08-01 09:17:45 UTC (15h 22m) | MYT 17:17:45 → 08:40:27
- **Memory:** 579.6M current / 590.2M peak ⚠️ (lihat escalation di atas)
- **CPU:** 7h 2min 17.9s total ≈ **45.8%** avg — dalam julat baseline 45–47% ✓

## 2) API Health — ✅
- `GET /health` → **HTTP 200** · `{"status":"ok","camera_configured":true,"camera_enabled":true,"camera_name":"entrance","free_disk_gb":345.8,"free_disk_pct":94.0,"models_present":true}`

## 3) Dashboard — ⚠️ Label berbeza dari yang dijangkakan (kosmetik sahaja)
- `/dashboard` → **HTTP 200**
- `Time (MYT)` → **ADA** (20×) ✓
- Literal `Gender (est.)` / `Age (est.)` → **TIDAK wujud** sebagai teks label. Yang ada: header **"Gender"** & **"Age"** (20× setiap satu), dengan tanda "(est.)" pada *nilai* (cth: `Male (est.)` 8×, `Female (est.)` 1×, `0-2 (est.)`, `25-32 (est.)` dsb) + tooltip *"AI estimate, ±5 years accuracy; display only"*. Maklumat est. tetap kelihatan, cuma format label tak sama dengan spec. Tidak escalate (bukan isu fungsi).

## 4) CSV — ✅
- `/api/events/csv` → **HTTP 200**
- Header: `Event ID,Camera,Type,Timestamp (UTC),Confidence,Snapshot,Face Snapshot` — **"Face Snapshot" ADA** ✓

## 5) Event Data — ✅ Tiada gap post-D.6
| Metrik | Bilangan |
|---|---|
| Jumlah event (23 Jul – 01 Ogos 2026) | **947** |
| Dengan `face_snapshot_path` | **413** |
| Dengan gender+age (sepanjang masa) | **338** (Female 106, Male 232) |
| **Post-D.6** (≥ 2026-07-29 12:00 UTC; D.6 deployed 27–29 Jul ikut mtime sumber) | **390** |
| — post-D.6 dengan gender+age | 190 |
| — post-D.6 ada face TAPI tiada attrs (**gap sebenar**) | **0** ✅ |
| — post-D.6 tanpa face (null attrs — jangkaan, tiada muka dikesan) | 200 |
| Pre-D.6 (attribut kosong = sejarah, bukan gap) | 557 |

- Event terakhir: **2026-08-01 12:57:25 UTC** (08-02 00:57 MYT) — senyap ~11.7 jam; hujung minggu, munasabah.
- Konsisten: event terakhir dgn face (12:56:39Z) ada attrs (Female, 4-6); tanpa face → null. **Zero genuine post-D.6 gaps.**

## 6) Face Crops — ✅
- `/mnt/cctv/faces/`: **413 fail, 9.3 MB** (padan 413 event dgn face_snapshot_path)

## 7) Resources — ⚠️ MEMORI (lihat escalation)
- **CPU:** 45.7% (ps) ≈ 45.8% (systemd) — baseline 45–47% ✓
- **RSS:** 588.8 MB (VmRSS 602,924 kB) | systemd current 579.6 MB | peak 590.2 MB — **baseline 296–435 MB → jauh melebihi** ⚠️
- VmSize 2,361 MB (virtual, termasuk reserved address space — tidak membimbangkan)

## 8) Security — ✅ Bersih, tiada credential terdedah
- Sejak service start: **551** baris rtsp URL → **551 masked** (`:***@`), **0 unmasked**
- Jam terakhir: **20** baris → **20 masked, 0 unmasked**
- Tiada pendedahan kredential. Journal TIDAK diubah.

## 9) Errors / Anomalies — ✅ Tiada error; warning = pola TC74 biasa
- **ERROR/Traceback/Exception: 0** (sejak start & jam terakhir)
- **550 warnings** = **275 kitaran disconnect** (`End of video stream` / `Stream disconnected` / `Reconnecting` / `Reopening`) ≈ 18/jam + decode warning h264 (`bytestream -5`) — **pola dikenali kamera Tapo TC74**, auto-recovery berjaya setiap kali (reconnect 5 saat), **0 restart**, 0 crash. Tidak materially worse.

## 10) Retention Timer — ✅
- Next run: **Sun 2026-08-02 20:00:00 UTC** (19h lagi) = **Mon 04:00 MYT** ✓ (jangkaan harian 20:00 UTC / 04:00 MYT)
- Last run: Sat 2026-08-01 20:00:40 UTC (4h 40m lalu) — on schedule

## Bonus: Disk
- `/mnt/cctv`: 346 GB free (1% used) — sihat

---

**Rumusan:** Semua fungsi stabil — service aktif, API 200, CSV & retention OK, **0 error, 0 crash, 0 gap post-D.6, 0 credential terdedah**. Satu-satunya isu: **memori worker ~589 MB (peak 590 MB) — jauh atas baseline 296–435 MB, hampir ambang 600 MB**. Sila pantau laporan seterusnya: kalau RSS melepasi 600 MB atau terus naik tanpa plateau, itu pengesahan kebocoran memori dan perlu tindakan (cth. semak `main.py.before-memfix` / D.6 inference path). Buat masa ini: **ESCALATION (memori), selebihnya BASELINE STABLE.**
