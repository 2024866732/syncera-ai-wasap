# Cron Job: cctv-baseline-6h-monitor

**Job ID:** 2aeb2f897d8d
**Run Time:** 2026-08-02 18:54:02
**Schedule:** every 360m

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

You are a passive monitoring agent for the HAFJET CCTV Worker on office PC 100.121.94.41. Run read-only checks only. Do NOT restart services, edit files/configurations, download models, rotate/delete logs, alter retention, expose the API, or make any HAFJET/Azure/WhatsApp/Telegram changes.

Use SSH as hafizi145. First test connectivity. If unreachable, report that clearly and stop.

Check and report, with every time shown as UTC plus MYT (UTC+8):
1) Worker: exact scoped command `sudo -n /usr/bin/systemctl status cctv-worker` (no extra systemctl args). Report active state, PID, uptime, memory/peak where visible.
2) API health: curl `http://127.0.0.1:8091/health`.
3) Dashboard: HTTP status for `/dashboard`; verify visible HTML labels `Time (MYT)`, `Gender (est.)`, `Age (est.)`; do not claim a label exists without checking.
4) CSV: HTTP status for `/api/events/csv` and verify its header contains `Face Snapshot`.
5) Event data: inspect recent events, count total, events with face_snapshot_path, and post-D.6 events with gender+age. Clearly distinguish historical pre-D.6 missing attributes from any post-D.6 gaps.
6) Face crops: file count and total disk usage of `/mnt/cctv/faces/`.
7) Resources: worker CPU/RSS plus systemd memory/peak. Track trend against prior known baseline: CPU about 45–47%, RSS around 296–435 MB. Escalate if RAM rises continuously across reports without plateau, if RSS exceeds 600 MB, or CPU sustains above 80%.
8) Security: inspect only NEW/latest worker journal lines for RTSP URLs and report masked vs unmasked counts without printing credentials. Expected new format contains `:***@`. Escalate immediately if an unmasked credential appears. Do NOT modify journal history.
9) Errors/anomalies: count recent ERROR/Traceback/Exception lines; separately report RTSP/decode/disconnect warnings as known Tapo TC74 camera-firmware behaviour unless materially worse. Escalate crashes, worker inactive/restarts, API failure, security exposure, or significant resource spike.
10) Retention: use read-only `systemctl list-timers cctv-face-retention.timer --no-pager` (non-sudo) and report next run, expected daily 20:00 UTC / 04:00 MYT.

Privacy: no facial recognition, identity matching, watchlists, embeddings, or biometric tracking. Gender/age remain display-only AI estimates (known ~75% accuracy; not facts). Keep report concise and factual. If all stable, state BASELINE STABLE; otherwise clearly state ESCALATION REQUIRED and why.

## Response

I have all the data needed. Compiling the report.

**HAFJET CCTV Worker Health Report — PC 100.121.94.41**
Generated: **18:53 UTC / 02:53 MYT, 2026-08-02**

Connectivity: ✅ SSH OK (`hafjet-pc-office`), all checks read-only.

---

**1) Worker service** — `sudo` is not passwordless, so used `systemctl status` (read-only). 
- Active: `active (running)`, Main PID **2612176** (python), uptime **1 day 9h** (since `2026-08-01 09:17 UTC` / `17:17 MYT`)
- Systemd memory: **674.2M** (peak **689M**)
- Total CPU so far 15h22m across uptime.

**2) API health** — `/health` = `{"status":"ok","camera_configured":true,"camera_enabled":true,"camera_name":"entrance","free_disk_gb":342.3,"free_disk_pct":93.0,"models_present":true}` ✅

**3) Dashboard** — `/dashboard` = **HTTP 200** ✅
- Label `Time (MYT)` — **present** ✅
- `Gender` and `Age` labels **present**; the literal string "Gender (est.)" is split in HTML — the "(est.)" appears on the *value* badges (`Male (est.)`, `25-32 (est.)`), not the header label. Functionally displayed; flagged for accuracy, not a defect.

**4) CSV** — `/api/events/csv` = **HTTP 200** ✅ Header = `Event ID,Camera,Type,Timestamp (UTC),Confidence,Snapshot,Face Snapshot` — contains **`Face Snapshot`** ✅

**5) Event data** (`/mnt/cctv/db/cctv_events.db`)
- **Total: 1,013** | with `face_snapshot_path`: **440** | with gender+age: **365**
- **Pre-D.6** (Jul 23–26): 225 events — all missing gender/age (expected historical, pre-attribute). ✅
- **Post-D.6** (≥ Jul 27): 788 events; **0 events have a face snapshot but missing gender/age** → **no real post-D.6 attribute gaps.** ✅
- Recent events show person-detected without face crops (no face confidently located) — normal.

**6) Face crops** — `/mnt/cctv/faces/` = **440 files**, **11M** total. ✅

**7) Resources — trend vs baseline**
- Worker RSS: **≈604 MB** (ps `RSS 618268 KB`) | effective ~670 MB including service overhead
- Systemd: **674.2M / peak 689.2M** | CPU **45.7%**
- Baseline was **RSS 296–435 MB**. Current is **well above 600 MB threshold** and rising across reports — consistent with a slow memory climb since the Jul 31/Aug 1 restart.

**8) Security** — latest worker journal: **152 RTSP URLs, all masked** (contain `:***@`); **0 unmasked** credentials. ✅ No exposure.

**9) Errors/anomalies**
- **0** ERROR / Traceback / Exception lines.
- Reconnect/decode warnings in 24h: stream-disconnect 457 · read-error/reopen 457 · h264 decode 394 — these are the **known Tapo TC74 camera-firmware reconnect churn** (cyclical ~every few minutes), not crashes. Worker stays active throughout.
- Retention timer: next run **2026-08-02 20:00 UTC / 04:00 MYT** (in ~1h), last ran 01 Aug — on daily schedule. ✅

---

**⚠️ ESCALATION REQUIRED — MEMORY**
Worker RSS/memory is at **~674M (RSS ~604 MB, systemd peak 689M)**, exceeding the **600 MB escalation threshold** and far above the prior 296–435 MB plateau after ~1.3 days uptime. CPU (45.7%) and everything else are stable, but this is a persistent resource climb from the low-water mark. Recommend a supervised restart of `cctv-worker` (when Tuan approves — I made no changes) and a memory-trend check on the next run to see whether the leak resumes.

All other functional health (API, dashboard, CSV, events integrity, security, retention) is **BASELINE STABLE**.

[Dipatuhi — Takde sebarang identiti/biometrik/FaceID ya, gender/age kekal anggaran paparan je server ~75% kena, bukan fakta. — Hermes]
