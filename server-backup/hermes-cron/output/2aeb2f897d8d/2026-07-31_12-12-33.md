# Cron Job: cctv-baseline-6h-monitor

**Job ID:** 2aeb2f897d8d
**Run Time:** 2026-07-31 12:12:33
**Schedule:** every 360m

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

You are a passive monitoring agent for the HAFJET CCTV Worker on office PC 100.121.94.41. Run read-only checks only. Do NOT restart services, edit files/configurations, download models, rotate/delete logs, alter retention, expose the API, or make any HAFJET/Azure/WhatsApp/Telegram changes.

Use SSH as hafizi145. First test connectivity. If unreachable, report that clearly and stop.

Check and report, with every time shown as UTC plus MYT (UTC+8):
1) Worker: exact scoped command `sudo -n /usr/bin/systemctl status cctv-worker` (no extra systemctl args). Report active state, PID, uptime, memory/peak where visible.
2) API health: curl `http://127.0.0.1:8091/health`.
3) Dashboard: HTTP status for `/dashboard`; verify visible HTML labels `Time (MYT)`, `Gender (est.)`, `Age (est.)`; do not claim a label exists without checking.
4) CSV: HTTP status for `/api/events/csv` and verify its header contains `Face Snapshot`.
5) Event data: inspect recent events, count total, events with face_snapshot_path, and post-D.6 events with gender+age. Clearly distinguish historical pre-D.6 missing attributes from any post-D.6 gaps.
6) Face crops: file count and total disk usage of `/mnt/cctv/faces/`.
7) Resources: worker CPU/RSS plus systemd memory/peak. Track trend against prior known baseline: CPU about 45–47%, RSS around 296–435 MB. Escalate if RAM rises continuously across reports without plateau, if RSS exceeds 600 MB, or CPU sustains above 80%.
8) Security: inspect only NEW/latest worker journal lines for RTSP URLs and report masked vs unmasked counts without printing credentials. Expected new format contains `:***@`. Escalate immediately if an unmasked credential appears. Do NOT modify journal history.
9) Errors/anomalies: count recent ERROR/Traceback/Exception lines; separately report RTSP/decode/disconnect warnings as known Tapo TC74 camera-firmware behaviour unless materially worse. Escalate crashes, worker inactive/restarts, API failure, security exposure, or significant resource spike.
10) Retention: use read-only `systemctl list-timers cctv-face-retention.timer --no-pager` (non-sudo) and report next run, expected daily 20:00 UTC / 04:00 MYT.

Privacy: no facial recognition, identity matching, watchlists, embeddings, or biometric tracking. Gender/age remain display-only AI estimates (known ~75% accuracy; not facts). Keep report concise and factual. If all stable, state BASELINE STABLE; otherwise clearly state ESCALATION REQUIRED and why.

## Response

All checks complete. Here is the full report.

---

# 📋 HAFJET CCTV Worker — Monitoring Report
**Host:** hafjet-pc-office (100.121.94.41) · **Run:** 2026-07-31 12:07 UTC = **20:07 MYT** · Read-only checks only · No changes made

## ⚠️ VERDICT: ESCALATION REQUIRED

**Sebab utama:** ada **pendedahan kredential RTSP dalam journal history** (tempoh 24 Jul – 27 Jul), walaupun format semasa dah bertukar kepada masked. Satu lagi **watch item**: RSS naik tinggi (597 MB, dekat garis 600 MB).

---

## 1) Worker Status — ✅ ACTIVE
| Item | Nilai |
|---|---|
| State | `active (running)` — Main PID **1217555** (python) |
| Uptime | Sejak **2026-07-31 05:27:54 UTC = 13:27:54 MYT** (6j 38m) |
| Restart | **Clean restart** (`Stopping… Deactivated successfully… Started`) — bukan crash, tiada OOM. Instance sebelum berjalan 1d 37min, peak 956.9M |
| Memory (systemd) | 698.7M (peak 705M) — termasuk page cache |
| CPU | 3j 01m 59s total ≈ **45.6% purata** — dalam baseline (45–47%) ✓ |

## 2) API Health — ✅ OK
`GET /health` → **200**: `status=ok`, camera `entrance` configured+enabled, models present, **347.8 GB free (94.5%)**.

## 3) Dashboard — ⚠️ Label wording berbeza
`/dashboard` → **HTTP 200**.
- `Time (MYT)` — ✅ **ada**, 20× dalam HTML
- `Gender (est.)` — ❌ **string tepat tak wujud**; label sebenar `Gender` sahaja
- `Age (est.)` — ❌ **string tepat tak wujud**; label sebenar `Age` sahaja
- `(est.)` muncul dalam nilai badge (`Male (est.)`, `8-12 (est.)`) dan `title='AI estimate, ±5 years accuracy; display only'`

Fungsional OK, cuma wording label berbeza daripada senarai semak.

## 4) CSV — ✅ OK
`/api/events/csv` → **200**. Header: `Event ID,Camera,Type,Timestamp (UTC),Confidence,Snapshot,Face Snapshot` — **`Face Snapshot` ✓ hadir**. 847 baris data: 364 berisi, 483 kosong (padan dengan DB).

## 5) Event Data — ✅ Tiada gap post-D.6
- Jumlah: **847** (23 Jul 11:38 → 31 Jul 11:00 UTC)
- Dengan `face_snapshot_path`: **364**
- Sempadan D.6 (event gender+age terawal): **2026-07-27 04:16:49 UTC = 12:16 MYT**
- **Post-D.6** (595): 289 ada gender+age (semua ada face crop), **306 tak ada face crop → tak ada attrs (normal, muka tak dikesan)**, **0** event ada face crop tapi gender/age hilang → **tiada gap data**
- **Pre-D.6** (252): semua 252 tanpa gender/age — **historical, dijangka**

## 6) Face Crops — ✅ OK
`/mnt/cctv/faces/` → **364 fail, 8.0 MB** (konsisten dengan DB).

## 7) Resources — ⚠️ WATCH (memori tinggi, tapi plateau)
- RSS: **597 MB** (VmRSS 611,364 kB) — **VmHWM = VmRSS**, 2 sampel 15s serupa → **stable/plateaued, bukan naik berterusan**
- Banding baseline (296–435 MB): naik **~37–100%** — dekat garis 600 MB tapi **belum melebihi**
- CPU: 45.6% purata — normal; instant 0% (idle antara deteksi)
- Tiada OOM dalam dmesg/kernel log

## 8) Security — 🚨 PENEMUAN PENTING
- Format baru `:***@` **berfungsi penuh**: **905 baris masked dalam 24 jam terakhir, 0 unmasked** ✓
- **TAPI**: journal history ada **2,496 baris unmasked** dengan password RTSP sebenar (8 aksara) dalam tempoh:
  - **Mula:** 2026-07-24 12:12 UTC = 20:12 MYT
  - **Tamat:** 2026-07-27 10:49 UTC = 18:49 MYT (~2j 22.5j)
- Kredential **tidak dipaparkan dalam laporan ini**. Journal history **tidak disentuh** (ikut arahan).

## 9) Errors / Anomalies — ✅ Tiada crash
- ERROR/Traceback/Exception (24j): **0**
- H.264 decode warning: 392 · End-of-stream: 452 · Disconnect: 452 (24j) — ~19–31 sejam, **stabil**, konsisten dengan **known Tapo TC74 firmware behaviour** — bukan lebih teruk
- Tiada worker restart mengejut; restart 05:27 UTC = restart bersih

## 10) Retention — ✅ On schedule
`cctv-face-retention.timer`: **next Fri 2026-07-31 20:00:00 UTC = 04:00 MYT (Sabtu)** — tepat seperti dijangka (7j lagi).

---

## 🚨 Tindakan dicadangkan (untuk Tuan — bukan saya)
1. **Rotate password RTSP camera Tapo** — ia telah wujud plaintext dalam journal selama ~3 hari (24–27 Jul). Walaupun log kini masked, sesiapa dengan akses journal boleh tarik kredential lama. Selepas tukar, update `.env` (rw------- ✓).
2. **Pantau memori** — RSS 597 MB dekat garis 600 MB; walaupun plateau sekarang, jika laporan seterusnya naik melebihi 600 MB atau naik berterusan, perlu siasat leak/konfigurasi.

**Status ringkas:** semua fungsi sihat (API, dashboard, CSV, retention, tiada crash, tiada gap data) — tetapi pendedahan kredential bersejarah + memori tinggi menjadikan laporan ini **ESCALATION REQUIRED**.
