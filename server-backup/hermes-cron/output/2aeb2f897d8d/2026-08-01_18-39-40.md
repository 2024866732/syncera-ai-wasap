# Cron Job: cctv-baseline-6h-monitor

**Job ID:** 2aeb2f897d8d
**Run Time:** 2026-08-01 18:39:40
**Schedule:** every 360m

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

You are a passive monitoring agent for the HAFJET CCTV Worker on office PC 100.121.94.41. Run read-only checks only. Do NOT restart services, edit files/configurations, download models, rotate/delete logs, alter retention, expose the API, or make any HAFJET/Azure/WhatsApp/Telegram changes.

Use SSH as hafizi145. First test connectivity. If unreachable, report that clearly and stop.

Check and report, with every time shown as UTC plus MYT (UTC+8):
1) Worker: exact scoped command `sudo -n /usr/bin/systemctl status cctv-worker` (no extra systemctl args). Report active state, PID, uptime, memory/peak where visible.
2) API health: curl `http://127.0.0.1:8091/health`.
3) Dashboard: HTTP status for `/dashboard`; verify visible HTML labels `Time (MYT)`, `Gender (est.)`, `Age (est.)`; do not claim a label exists without checking.
4) CSV: HTTP status for `/api/events/csv` and verify its header contains `Face Snapshot`.
5) Event data: inspect recent events, count total, events with face_snapshot_path, and post-D.6 events with gender+age. Clearly distinguish historical pre-D.6 missing attributes from any post-D.6 gaps.
6) Face crops: file count and total disk usage of `/mnt/cctv/faces/`.
7) Resources: worker CPU/RSS plus systemd memory/peak. Track trend against prior known baseline: CPU about 45–47%, RSS around 296–435 MB. Escalate if RAM rises continuously across reports without plateau, if RSS exceeds 600 MB, or CPU sustains above 80%.
8) Security: inspect only NEW/latest worker journal lines for RTSP URLs and report masked vs unmasked counts without printing credentials. Expected new format contains `:***@`. Escalate immediately if an unmasked credential appears. Do NOT modify journal history.
9) Errors/anomalies: count recent ERROR/Traceback/Exception lines; separately report RTSP/decode/disconnect warnings as known Tapo TC74 camera-firmware behaviour unless materially worse. Escalate crashes, worker inactive/restarts, API failure, security exposure, or significant resource spike.
10) Retention: use read-only `systemctl list-timers cctv-face-retention.timer --no-pager` (non-sudo) and report next run, expected daily 20:00 UTC / 04:00 MYT.

Privacy: no facial recognition, identity matching, watchlists, embeddings, or biometric tracking. Gender/age remain display-only AI estimates (known ~75% accuracy; not facts). Keep report concise and factual. If all stable, state BASELINE STABLE; otherwise clearly state ESCALATION REQUIRED and why.

## Response

All checks complete. Compiling the report.

---

# HAFJET CCTV Worker — Monitoring Report
**Host:** hafjet-pc-office (100.121.94.41) · **Checked:** 2026-08-01 18:32–18:39 UTC (**02:32–02:39 MYT**) · Read-only, no changes made

## 1) Worker Status — ✅ ACTIVE
- State: `active (running)` · Main PID **2612176** (`.venv/bin/python -m app.main`) · Tasks 24
- Started: **2026-08-01 09:17:45 UTC (17:17:45 MYT)** — uptime ~9h21m at check · `NRestarts=0`
- Confirmed via exact command `sudo -n /usr/bin/systemctl status cctv-worker` (exit 0). Prior cycle verified the 09:17 start was a clean stop→start (09:07:35 → 09:17:45 UTC), **not a crash**

## 2) API Health — ✅ OK (HTTP 200)
`status:ok · camera=entrance · enabled:true · free_disk:345.8 GB (94%) · models_present:true`

## 3) Dashboard — ✅ HTTP 200 (label wording differs)
- `Time (MYT)` — **present** (verified via grep)
- `Gender (est.)` / `Age (est.)` — **NOT present as literal strings** (verified, case-insensitive). Actual HTML renders labels `Gender` / `Age` with estimate badges (e.g. `Male (est.)`, `25-32 (est.)`) and `title='AI estimate, ±5 years'`. Estimates are visible, but the exact requested label strings do not exist — not claiming otherwise (matches prior cycles)

## 4) CSV — ✅ HTTP 200, header OK
`Event ID,Camera,Type,Timestamp (UTC),Confidence,Snapshot,Face Snapshot` → **contains "Face Snapshot"** ✓

## 5) Event Data (SQLite `cctv_events.db`, read-only)
- **Total: 947** (2026-07-23 11:38 → 2026-08-01 12:57:25 UTC; +4 vs prior report)
- With `face_snapshot_path`: **413** · With gender+age: **338** (always paired)
- **D.6 go-live** = earliest gender event **2026-07-27 04:16:49 UTC**
- **Post-D.6 gaps: ZERO.** Face-crop counts == attribute counts every day since go-live (07-28: 53/53, 07-29: 75/75, 07-30: 109/109, 07-31: 23/23, 08-01: 46/46)
- All 75 face-without-attributes events are **historical pre-D.6** (46 on 07-25, 16 on 07-26, 13 on 07-27 03:10–04:06 UTC = rollout window **before** 04:16:49 go-live) — expected, not a defect
- Note: last event 12:57 UTC (~5.7h quiet); worker loop confirmed alive via reconnect cycles at 18:33 UTC — normal quiet period

## 6) Face Crops — ✅ **413 files, 9.3 MB** (`/mnt/cctv/faces/`) — matches DB count

## 7) Resources — ⚠️ ELEVATED, **plateau confirmed**
- CPU **45.6%** (within 45–47% baseline ✓)
- ps RSS **602,924 KB** (588.8 MiB; 602.9 MB decimal) · systemd MemoryCurrent **579.8 MiB**, MemoryPeak **585.0 MiB**
- **Trend (9 samples):** 30-min tracker 15:02–18:37 UTC → MemoryCurrent 578.5–582.0 MiB band, Peak constant 585.0 MiB; vs prior report 12:31 UTC (RSS 602,644 KB, Peak 584.9 MiB) → **flat over 6h+. Not rising continuously — stable elevated plateau, not a leak**

## 8) Security — ✅ All masked
24h journal: **892 RTSP lines → 892 masked (`:***@`), 0 unmasked** — new format confirmed (`hafizi145%40gmail.com:***@192.168.1.94:554/stream1`), **no credential exposure**

## 9) Errors/Anomalies — ✅ No crashes
- ERROR/Traceback/Exception: **0** in 24h · starts/stops: 1 (the clean 09:17 UTC start)
- RTSP/decode/disconnect lines: 1361 (h264 decode errors, end-of-stream, reconnect cycles) — **known Tapo TC74 camera-firmware behaviour**, consistent with prior 1187, not materially worse

## 10) Retention Timer — ✅ On schedule
- Next: **2026-08-01 20:00:00 UTC (2026-08-02 04:00 MYT)** — ~1h26m away · Last: 2026-07-31 20:00:10 UTC · matches expected daily 20:00 UTC / 04:00 MYT

---

# ⚠️ ESCALATION REQUIRED — memory still above threshold (continued)

**Primary trigger:** RSS **602.9 MB (decimal reading per established convention)** remains above the 600 MB escalation line and far above the 296–435 MB baseline — same condition flagged last cycle (unchanged, not worsened).

**Key update (good news):** plateau is now **confirmed** — RSS flat across two reports 6h apart (602,644 → 602,924 KB) and MemoryCurrent oscillating in a tight 578.5–582.0 MiB band across 9 samples over 3.5h, with MemoryPeak pinned at 585.0 MiB. This is a **stable elevated state, NOT a growing memory leak**, and CPU is within baseline.

**Everything else stable:** worker active (no crash), API/dashboard/CSV OK, security clean (892/892 masked), zero post-D.6 attribute gaps, retention on schedule.

**Recommendation for Tuan Hafizi (no changes made):** investigate the elevated-but-flat RSS (likely tied to today's D.6/memfix state — `main.py.before-memfix` exists in the project dir) at leisure; continue monitoring for any change from the plateau. No emergency action needed.

Privacy note: no facial recognition/identity tracking performed — gender/age remain display-only AI estimates (~75% accuracy, not facts).
