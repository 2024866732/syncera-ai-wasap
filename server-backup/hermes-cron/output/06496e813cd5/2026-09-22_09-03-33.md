# Cron Job: azure-credit-daily-check

**Job ID:** 06496e813cd5
**Run Time:** 2026-09-22 09:03:32
**Schedule:** 0 9 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Run the Azure credit check script and report if costs are spiking.

Steps:
1. Run: bash /home/hafizi145/.hermes/scripts/azure_credit_check.sh
2. If the cost exceeds $50 USD this month, send a warning: "⚠️ Azure for Students credit alert: $X spent this month. Consider reviewing usage."
3. If cost is $0-$50, just log silently — no need to bother the user.
4. If the script fails (e.g., Azure CLI not available), report the error.

Output format: Only send a message to the user if there's a warning or error. Otherwise stay silent.

## Response

**⚠️ Tiada spike — TAPI script monitor tu ROSAK (lapor $0.00 palsu)**

**Cost sebenar (sahih, Microsoft CostManagement API):**
- Month-to-date: **$6.25 USD** (22 hari) ≈ **$0.296/hari**, **flat** — tiada trend naik
- Subscription: `Azure for Students` (UiTM), state `Enabled`
- Jauh bawah threshold $50 → **takda credit warning**

**Yang rosak:**
- `azure_credit_check.sh` cetak `$0.00` — itu BUKAN "percuma", itu data kosong
- Punca: `az consumption usage list` pulangkan `pretaxCost: "None"` untuk **semua 173 rows** (preview API tak bagi nilai kos)
- Script telan error: `2>/dev/null` + `except: print('0.00')` → **silent failure**. Alarm >$50 tu takkan pernah bunyi, walau berapa pun spend.

**Fix cadangan (script cron = prod, so ambo TAK ubah tanpa approval):**
```diff
-OUTPUT=$(az consumption usage list 2>/dev/null | grep -v "WARNING" | python3 -c "
+OUTPUT=$(az rest --method post \
+  --url "https://management.azure.com/subscriptions/f9de0896-0015-4001-adbf-64814ec0ad34/providers/Microsoft.CostManagement/query?api-version=2023-11-01" \
+  --body '{"type":"ActualCost","timeframe":"MonthToDate","dataset":{"granularity":"None","aggregation":{"totalCost":{"name":"Cost","function":"Sum"}}}}' \
+  | python3 -c "
```
Dua benda kena buang: `2>/dev/null` (sorok error) dan `except: print('0.00')` (palsukan fail jadi kosong). Biar fail keras → baru error nampak macam sepatutnya.

Nak ambo tampal + test (Audit → Approve → Deploy)? Reply **A** kalau nak.
