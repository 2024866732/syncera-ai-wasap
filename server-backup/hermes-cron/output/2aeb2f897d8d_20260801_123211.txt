Cronjob Response: cctv-baseline-6h-monitor
(job_id: 2aeb2f897d8d)
-------------

All checks complete. Compiling the report.

---

# HAFJET CCTV Worker — Monitoring Report
**Host:** hafjet-pc-office (100.121.94.41) · **Checked:** 2026-08-01 12:27–12:31 UTC (**20:27–20:31 MYT**)

## 1) Worker Status — ✅ ACTIVE
- **State:** `active (running)` · Main PID **2612176** (python `-m app.main`)
- **Started:** 2026-08-01 **09:17:45 UTC (17:17:45 MYT)** — uptime ~3h10m at check time
- **Restarts (systemd):** 0 · Note: service was cleanly stopped 09:07:35 UTC and started 09:17:45 UTC today (deactivated successfully, **not a crash**)
- ⚠️ `sudo -n /usr/bin/systemctl status` required interactive auth (no NOPASSWD over SSH) — same fields obtained via non-sudo `systemctl status`/`systemctl show`

## 2) API Health — ✅ OK (HTTP 200)
`status:ok · camera=entrance · configured:true · enabled:true · free_disk:345.9 GB (94%) · models_present:true`

## 3) Dashboard — ✅ HTTP 200
- Label `Time (MYT)` — **present** (20×)
- ⚠️ Exact strings `Gender (est.)` and `Age (est.)` — **NOT present in HTML**. Gender/age render as label `Gender`/`Age` with value badges `Male (est.)` / `25-32 (est.)` (20× `(est.)`). Estimates are visible, but the exact labels requested do not exist — not claiming otherwise.

## 4) CSV — ✅ HTTP 200, header OK
`Event ID,Camera,Type,Timestamp (UTC),Confidence,Snapshot,Face Snapshot` → **contains "Face Snapshot"** ✓

## 5) Event Data (SQLite `cctv_events.db`, read-only)
- **Total events:** 943 (2026-07-23 → 2026-08-01 10:45 UTC)
- **With face_snapshot_path:** 412
- **With gender + age_range:** 337 (always paired)
- **Post-D.6:** go-live = earliest gender event **2026-07-27 04:16:49 UTC**. The only post-D.6 "face-without-attributes" events are **13 events on 2026-07-27 03:10–04:06 UTC — all BEFORE go-live** (rollout window). After 04:16 UTC Jul 27: face-crop counts == gender counts every day (53/53, 75/75, 109/109, 23/23, 45/45) → **zero post-D.6 gaps**
- **Pre-D.6 (historical):** 225 events, 62 faces, 0 gender — expected missing attributes, not a defect

## 6) Face Crops — ✅ 412 files, **9.3 MB** (`/mnt/cctv/faces/`) — matches DB count

## 7) Resources — ⚠️ ELEVATED vs baseline
- CPU **45.7–45.8%** (within 45–47% baseline)
- RSS **602,644 KB = 588.5 MiB / 602.6 MB decimal** (two samples 45s apart identical → plateau, not climbing intra-session)
- systemd: MemoryCurrent **574.9 MiB**, MemoryPeak **584.9 MiB**
- Prior baseline: RSS 296–435 MB → current is **well above baseline and RSS (decimal) crosses the 600 MB escalation line** (MiB interpretation: 588.5 < 600, within 12 MiB of it). Previous instance also peaked ~582M → plateau at a higher level.

## 8) Security — ✅ All masked
- 835 RTSP lines in last 24h journal: **835 masked (`:***@`), 0 unmasked** — no credential exposure

## 9) Errors/Anomalies — ✅ No crashes
- ERROR/Traceback/Exception: **0** in 24h
- Warnings: 834; RTSP/decode/disconnect lines: 1187 (h264 decode errors, end-of-stream, reconnecting) — **known Tapo TC74 camera-firmware behaviour**, not materially worse

## 10) Retention Timer — ✅ On schedule
- Next: **2026-08-01 20:00:00 UTC (2026-08-02 04:00 MYT)** — ~7h away · Last: 2026-07-31 20:00:10 UTC (16h ago) · matches expected daily 20:00 UTC / 04:00 MYT

---

# ⚠️ ESCALATION REQUIRED — memory above threshold

**Primary trigger:** RSS has risen from the documented 296–435 MB baseline to **~602.6 MB (decimal)** — crossing the 600 MB escalation line (588.5 MiB if read in binary units, still far above baseline and near threshold). The service also restarted cleanly today at 09:17 UTC (not a crash; NRestarts=0).

**Secondary observations (no action needed):**
- Dashboard does not contain literal `Gender (est.)` / `Age (est.)` labels — estimates render as `Male (est.)` / `25-32 (est.)` value badges
- `sudo -n systemctl status` blocked by sudoers config (interactive auth required) — monitoring used non-sudo equivalents
- 13 face crops from the D.6 rollout window (Jul 27 03:10–04:06 UTC) lack gender/age — historical, pre-go-live

**Recommendation for Tuan Hafizi (no changes made — read-only run):** investigate worker RSS growth vs the earlier ~300–435 MB baseline — likely tied to the today's D.6/memfix state; verify the process has plateaued at ~585 MiB or continue monitoring next cycle for a rising trend. Everything else (API, dashboard, CSV, security, retention, camera reconnects) is stable.

To stop or manage this job, send me a new message (e.g. "stop reminder cctv-baseline-6h-monitor").