# Cron Job: hafjet-pickup-reminder

**Job ID:** ebdae9cc10ab
**Run Time:** 2026-07-15 03:08:38
**Schedule:** 0 3 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Run the HAFJET pickup reminder script. Execute: python3 /home/hafizi145/.hermes/skills/software-development/hafjet-biz-ops/scripts/hafjet_pickup_reminder.py

Steps:
1. Load env vars from ~/.hermes/.env (GOOGLE_SHEET_ID, GOOGLE_SHEETS_CREDENTIALS, PICKUP_REMINDER_TAB, WHATSAPP_CLOUD_PHONE_ID, WHATSAPP_CLOUD_ACCESS_TOKEN, OWNER_PHONE).
2. The script reads the Google Sheet repair tab, filters STATUS_REPAIR == 'SIAP DIAMBIL', and sends a free-form WhatsApp reminder to each CUSTOMER (NO_PHONE column) — NOT the owner.
3. If WhatsApp returns error 470 (24h window), it logs and skips (owner will handle template later).
4. Sends an optional owner summary of pending count to OWNER_PHONE.
5. Report: how many sent, how many failed, any 470 blocks. Keep it concise.

## Response

========================================
**HAFJET PICKUP REMINDER — CRON REPORT**  
📅 Wed 15 Jul 2026 · 11:00 AM MYT  
========================================

### ✅ Sheet Access — OK
| Item | Status |
|------|--------|
| Google Sheet | ✅ Readable (`1T0FzNhkOTgyORFvllsc0pXz2xsZV2nkrwBBvzNYs9KE`) |
| Tab | `REPAIR BARU` |
| Total rows | 995 |
| **Pending pickup (Status=SIAP)** | **621 rows** |

### ❌ WhatsApp — BLOCKED
**Phone Number ID `107158292462704` is invalid.**  
→ Error `100 / subcode 33`: *"Object with ID '107158292462704' does not exist"*  
→ Previously flagged on 2026-07-14 as potentially stale — still broken today.

| Metric | Count |
|--------|-------|
| ✅ Sent successfully | **0** |
| ❌ HTTP 400 (invalid Phone ID) | **~229** (script timed out before finishing batch) |
| ⏭️ Skipped — no phone in sheet | **7** (ID-008, 081, 122, 130, 138, 150, 228) |
| ⏭️ 470 blocks (24h window) | **0** (never reached that check) |
| 📊 Owner summary | **Not sent** (can't send while Phone ID invalid) |

### 🛠️ Tuan kena buat
1. **Dapatkan Phone Number ID betul** dari Meta Business  
   → *Facebook Business → WhatsApp → API Setup → Phone Number ID*
2. **Update** `~/.hermes/.env`:
   ```
   WHATSAPP_PHONE_ID=<new_id_here>
   ```
3. **Test dry-run dulu**:
   ```bash
   set -a; source ~/.hermes/.env; set +a
   export GOOGLE_SHEET_ID="1T0FzNhkOTgyORFvllsc0pXz2xsZV2nkrwBBvzNYs9KE"
   export GOOGLE_SHEETS_CREDENTIALS="/home/hafizi145/.hermes/secrets/gsheet_sa.json"
   export PICKUP_REMINDER_TAB="REPAIR BARU"
   export PICKUP_REMINDER_STATUS_VALUE="SIAP"
   export OWNER_PHONE="60198021500"
   export WHATSAPP_CLOUD_PHONE_ID="$WHATSAPP_PHONE_ID"
   export WHATSAPP_CLOUD_ACCESS_TOKEN="$WHATSAPP_ACCESS_TOKEN"
   export DRY_RUN=1
   python3 ~/.hermes/skills/software-development/hafjet-biz-ops/scripts/hafjet_pickup_reminder.py
   ```
4. Kalau dry-run nampak ok, baru **remove `DRY_RUN=1`** dan jalankan betul-betul.

### 📌 Additional notes
- **621 SIAP tickets is a lot** — mungkin perlu cleanup status lama (banyak dari 2022).
- **Dual phone numbers** (e.g. `601111144636/0104163884`) dalam satu cell still not handled — akan fail send.
- **7 variant rows** `SEDIA DI AMBIL` also not caught by current filter (Status=SIAP only).
