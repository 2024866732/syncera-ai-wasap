# Cron Job: hafjet-pickup-reminder

**Job ID:** ebdae9cc10ab
**Run Time:** 2026-07-18 03:10:37
**Schedule:** 0 3 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Run the HAFJET pickup reminder script. Execute: python3 /home/hafizi145/.hermes/skills/software-development/hafjet-biz-ops/scripts/hafjet_pickup_reminder.py

Steps:
1. Load env vars from ~/.hermes/.env (GOOGLE_SHEET_ID, GOOGLE_SHEETS_CREDENTIALS, PICKUP_REMINDER_TAB, WHATSAPP_CLOUD_PHONE_ID, WHATSAPP_CLOUD_ACCESS_TOKEN, OWNER_PHONE).
2. The script reads the Google Sheet repair tab, filters STATUS_REPAIR == 'SIAP DIAMBIL', and sends a free-form WhatsApp reminder to each CUSTOMER (NO_PHONE column) — NOT the owner.
3. If WhatsApp returns error 470 (24h window), it logs and skips (owner will handle template later).
4. Sends an optional owner summary of pending count to OWNER_PHONE.
5. Report: how many sent, how many failed, any 470 blocks. Keep it concise.

## Response

**HAFJET Pickup Reminder — Cron Run Report**
📅 Saturday, July 18, 2026

---

**📊 Overview**

| Metric | Value |
|--------|-------|
| Tab | REPAIR BARU |
| Filter | Status = SIAP |
| Total rows | 995 |
| Pending pickup | 621 |
| WhatsApp sent ✅ | **0** |
| WhatsApp failed ❌ | **621** |
| No phone skipped ⚠️ | 9 |
| Owner summary | Gagal |

---

**🚨 Root Cause: WhatsApp Credential Invalid**

Direct API test against `107158292462704` returned:
```
code: 100, error_subcode: 33
→ "Object with ID '107158292462704' does not exist, cannot be loaded
   due to missing permissions, or does not support this operation."
```

**The Phone Number ID stored in `.env` (`WHATSAPP_PHONE_ID = 107158292462704`) is stale.** Meta may have:
- Changed the phone number assigned to the WABA
- Revoked or expired the access token
- Required reauthentication

No 470 (24h window) errors occurred — the credential itself is broken, so nothing was blocked by the customer window rule. Every single send attempt failed before reaching that stage.

---

**🔧 What Tuan Perlu Buat**

1. **Login to Meta Business Suite** → https://business.facebook.com
2. Navigate to **WhatsApp > API Setup** or **Business Account > WhatsApp Accounts**
3. Verify current **Phone Number ID** (may have changed if number was re-added)
4. Copy the new Phone Number ID
5. **Regenerate access token** if expired
6. Update `.env`:
   ```bash
   nano ~/.hermes/.env
   # Update:
   WHATSAPP_PHONE_ID="<new_phone_number_id>"
   WHATSAPP_ACCESS_TOKEN="<new_token>"
   ```
7. Test with a single dry run:
   ```bash
   DRY_RUN=1 python3 /home/hafizi145/run_pickup_reminder.py
   # If OK, real run:
   python3 /home/hafizi145/run_pickup_reminder.py
   ```

**Pending count is 621 tickets.** Recommend Tuan cleanup old SIAP data (many from 2022) before re-running, or the cron will need a 300s+ timeout again.
