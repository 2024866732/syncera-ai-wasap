# Cron Job: hafjet-pickup-reminder

**Job ID:** ebdae9cc10ab
**Run Time:** 2026-07-19 03:04:47
**Schedule:** 0 3 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Run the HAFJET pickup reminder script. Execute: python3 /home/hafizi145/.hermes/skills/software-development/hafjet-biz-ops/scripts/hafjet_pickup_reminder.py

Steps:
1. Load env vars from ~/.hermes/.env (GOOGLE_SHEET_ID, GOOGLE_SHEETS_CREDENTIALS, PICKUP_REMINDER_TAB, WHATSAPP_CLOUD_PHONE_ID, WHATSAPP_CLOUD_ACCESS_TOKEN, OWNER_PHONE).
2. The script reads the Google Sheet repair tab, filters STATUS_REPAIR == 'SIAP DIAMBIL', and sends a free-form WhatsApp reminder to each CUSTOMER (NO_PHONE column) — NOT the owner.
3. If WhatsApp returns error 470 (24h window), it logs and skips (owner will handle template later).
4. Sends an optional owner summary of pending count to OWNER_PHONE.
5. Report: how many sent, how many failed, any 470 blocks. Keep it concise.

## Response

✅ **Sheet connectivity verified** — 995 rows, 621 pending `SIAP`.

But WhatsApp credentials are **still broken** (4th consecutive cron run). Here's the report.

---

## 📦 Pickup Reminder — Cron Report (2026-07-19 11:00 MYT)

### Status: ⛔ ABORTED — No messages sent

### Pre-flight WhatsApp test
```
HTTP 400 — code: 100, subcode: 33
Object with ID '107158292462704' does not exist
```
Same credential failure as the **3 previous cron runs** (2026-07-14, 15, 17). The Phone Number ID `107158292462704` is stale/invalid.

Per the SOP escalation rule: *"If the SAME credential error fires across ≥2 consecutive cron runs, abort early — no batch attempt."*

### Sheet data (DRY_RUN = 1)
| Metric | Value |
|--------|-------|
| Total rows | 995 |
| Status filter | `SIAP` (not `SIAP DIAMBIL`) |
| Pending pickup | **621** |
| Would be sent | 608 |
| Skipped (no phone) | 13 |
| Would get 470 block | ~all (24h window) |

### Known issues (also documented)
- **7 rows** with `SEDIA DI AMBIL` (typo) escape the filter
- **Dual phone numbers** (e.g. `601111144636/0104163884`) will fail norm_phone
- **Non-MY numbers** (`189017751`, `168336997`) won't work
- Most SIAP tickets date from **Sept–Nov 2022**

### Recovery steps (Tuan Hafizi)
1. **Get correct Phone Number ID** from [Meta Business → WhatsApp → API Setup](https://business.facebook.com/wa/manage/).
2. Update `WHATSAPP_PHONE_ID` in `~/.hermes/.env`.
3. Export it as `WHATSAPP_CLOUD_PHONE_ID` before next cron run.
4. Optionally: clean up old 2022 SIAP statuses in the sheet.
5. For the 24h window issue, submit a pre-approved WhatsApp message template via Meta.
