Tuan Hafizi — MD of HAFJET. UiTM student. Prefers casual Malay+Kelantan dialect. Frustrated by over-explaining. GitHub: 2024866732. Screenshots=analyze first.
§
User preference: Concise, scannable answers (tables/bullets). One unit of work per run, max 20 tool calls, STOP & summarize. Run exactly one validation after fix. Never guess secrets — state "must be regenerated by you".
§
Azure deploy: Python zipfile (no zip cmd). Throttle 429→STOP, never loop. Region SE Asia. Never az webapp up (nulls settings). Oryx DELETES .py from /home/site/wwwroot→start.sh must `find /tmp /home -name webhook_listener.py` dynamically. Include ALL .py in ZIP (not hardcoded list). ZIP excludes .env,node_modules,__pycache__,.git,venv.
§
WhatsApp Bot: single source of truth = Azure env vars. Critical keys: OPENROUTER_MODEL, AI_ENABLED, BOT_ACTIVE. DB is fallback only. Always show diff+test before deploy; deploy only on explicit command.
§
HAFJET bot deployed with env-override config precedence. Effective model = nvidia/nemotron-3-super-120b-a12b:free. Dashboard shows effective values + today-only analytics with date picker. MODEL_DEBUG logging active.
§
FastAPI SPA: catch-all route must be LAST. Add `if full_path.startswith("api/")` guard before fallback.
§
FastAPI: `Dependencies(depends=[...])` runs auth but doesn't inject param. Use `Depends(...)` in function signature for injection. Route order matters.
§
VPS pick: Hetzner CX22 (€3.99/mo, Singapore DC) best value for Malaysia. Upgrade: CX32 (€8). Provider eval ladder: free first (OpenCode Zen Big Pickle) → OpenCode Go $10 → Gemini paid.
§
2026-07-04 fixes: get_current_staff must precede Depends() routes (NameError → exit 3); _process_message escalation path had undefined `loop2` instead of `loop_db` (silent webhook failure). appCommandLine: absolute path to antenv/bin/gunicorn works; cd && fails exit 127; bash -c wrapper alternative. ContainerStream often 0 bytes — use docker.log exit codes + local import test. Sprint v2.1.1 deployed.
§
User preference: Don't change appCommandLine before showing error logs. Deploy only on explicit approval. Show diff + test logs before any risky action.