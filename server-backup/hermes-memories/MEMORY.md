Tuan Hafizi — MD of HAFJET, UiTM on study leave. Malay+Kelantan dialect, tech English for code. Wants concise scannable output (tables/bullets), hates over-explaining and 'semua dah ok' without evidence. Rules: show diff+test logs before deploy, deploy only on explicit command, 1 task/run max 20 calls STOP, never guess secrets — state 'must be regenerated by you'.
§
SPX: entity_id=order['id'], show_secret for real phone, rate 0.3s/0.5s, cap 60/day, send window 08:00-21:00 MYT via ZoneInfo (server UTC), no Sunday guard, frontend SPXOrders session UI.
§
Git repo: https://github.com/2024866732/hafjet-whatsapp-bot.git (confirmed active — NOT syncera-ai-wasap)
§
Hermes WebUI: ~/hermes-webui, systemd hermes-webui.service, 0.0.0.0:8787. Tailscale tailnet: server 100.111.105.120. Setup: git clone → uv pip install -r requirements.txt → HERMES_WEBUI_PASSWORD in .env → ctl.sh start → sudo service hermes-webui start. .env is secret (read_file blocked, use terminal).
§
Context7 CLI: ctx7 docs exact 2-arg syntax only; extra keywords fail. Some queries blocked as false-positive long-process — use simpler query or web_search. Max 3 library calls/question.
§
Rules: show diff+test logs before deploy, deploy only on explicit approval, 1 unit/run max 20 calls STOP, never guess secrets (must be regenerated by Tuan). Scannable output (tables/bullets), no overexplaining without evidence.
§
External API defense: never trust resp.json() alone — use _safe_json() that returns {} instead of raising. Guard empty body, prefix (Shopee: )]}\'\n), unmatched brackets, parse failures. Log raw body(500) for debugging.
§
WS pong: send_json({'type': 'pong'}) not send_text('pong') — frontend JSON.parse requires valid JSON.
§
Kudu VFS DELETE with If-Match=* works for old asset cleanup after frontend rebuild
§
Frontend WS handler should silently ignore non-JSON messages (try/catch with no console.error)
§
Azure deploy: startup.txt overrides start.sh (must exclude). Clear __pycache__ in start.sh before gunicorn. az webapp deploy --type zip skips Oryx build. Set WEBSITES_CONTAINER_START_TIME_LIMIT=300. Use Kudu VFS PUT + stop/start. AAD Bearer token auth. No RETURNING. Version markers.